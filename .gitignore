# dependencies
/node_modules
/.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# env — .env.local holds the Supabase URL and anon key
.env*.local
.env

# vercel / typescript
.vercel
*.tsbuildinfo

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# the roster mapping lives in roster.tsv; the Numbers bundle it was
# filled in with is a 250KB binary copy of the same data
scripts/import/*.numbers
